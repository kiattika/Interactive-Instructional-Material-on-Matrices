# บันทึกการปรับปรุงเฟสที่ 1 (Phase 1 Update Notes)

**สื่อการสอน:** Interactive Matrix Solver (MatrixMaster Pro)
**ขอบเขตเฟสนี้:** ปรับปรุงเนื้อหา/ฟีเจอร์ตามที่ระบุ **โดยไม่มีระบบ login หรือระบบเก็บข้อมูลนักเรียนส่วนกลาง**
นักเรียนทุกคนเข้าศึกษาได้ทันทีจากลิงก์เดียวกัน ไม่ต้องสมัครสมาชิก ไม่ต้องใส่รหัสผ่าน (ระบบเก็บข้อมูลนักเรียน
รายบุคคลแบบข้ามเครื่อง ถูกเลื่อนไปเป็นเฟสที่ 2 ตามที่ตกลงกันไว้)

---

## 1. สรุปสิ่งที่เพิ่ม/แก้ไขในเฟสนี้

### 1.1 ขยายการแก้ระบบสมการเชิงเส้นสู่ระบบขั้นสูง (4×4)
- `src/types.ts`: ขยาย `SystemDimension` เป็น `'2x2' | '3x3' | '4x4'`
- `src/lib/matrixEngine.ts`:
  - `det()` รองรับเมทริกซ์ n×n ทั่วไปด้วย cofactor expansion (เดิมรองรับแค่ 2×2/3×3)
  - `solveLinearSystem()` เมื่อ n ≥ 4 จะหาคำตอบผ่าน RREF (Gaussian Elimination) แทนการหา Inverse
    แบบ Adjugate (ซึ่งจงใจไม่รองรับเกิน 3×3 ตามหลักสูตร สสวท.)
  - `verifySolution()` รองรับชื่อตัวแปร 4 ตัว (x, y, z, w)
  - `getGaussSteps()` และ `computeRREF()` เป็นฟังก์ชันทั่วไปอยู่แล้ว ไม่ต้องแก้ไข ใช้ได้ทันทีกับ 4×4
- **หน้าใหม่ `src/pages/HigherOrderLab.tsx`** (route `/higher-order-lab`, เมนู "ระบบสมการขั้นสูง (4x4)"):
  ห้องปฏิบัติการ 4×4 เฉพาะวิธี Gaussian Elimination พร้อมโจทย์ตัวอย่างวงจรไฟฟ้า 4 ลูป (คำตอบมีค่าติดลบ
  เพื่อฝึกตีความทิศทางกระแส) และคำอธิบายว่าทำไม Cramer/Inverse จึงไม่ใช้ที่ขนาดนี้
- **บทเรียนใหม่ 2 บท** ใน `src/lib/learningStore.ts`:
  - บทที่ 11: เมทริกซ์ในงานวิศวกรรมและไอซีที
  - บทที่ 12: ระบบสมการขั้นสูง (มากกว่า 3 ตัวแปร)
- ทดสอบแล้วด้วยกรณี unique / no-solution / infinite-solutions ทั้งหมด (ดูหัวข้อ 3)

### 1.2 คลังโจทย์ประยุกต์วิศวกรรม/ไอซีที (7 ข้อ)
ไฟล์ใหม่ `src/lib/engineeringProblems.ts` — ทุกโจทย์ตรวจสอบเชิงตัวเลขแล้วว่า det(A) ≠ 0 และคำตอบสอดคล้อง
บริบทจริง:

| ข้อ | บริบท | มิติ |
|---|---|---|
| วงจรไฟฟ้า 2 ลูป (Kirchhoff) | วิศวกรรมไฟฟ้า | 2×2 |
| แรงดึงในเส้นลวดโครงถัก | วิศวกรรมโครงสร้าง | 2×2 |
| วงจรไฟฟ้า 3 ลูป (มีคำตอบติดลบ) | วิศวกรรมไฟฟ้า | 3×3 |
| ผสมสารละลาย 3 ชนิด | วิศวกรรมเคมี | 3×3 |
| ถอดรหัส Hill Cipher | ไอซีที: Cybersecurity | 2×2 |
| ย้อนพิกัดก่อนแปลงภาพ | ไอซีที: Computer Graphics | 2×2 |
| สืบย้อน Load Balancing เซิร์ฟเวอร์ | ไอซีที: Network | 3×3 |

นำไปใช้แล้วใน:
- **Exercises.tsx**: เพิ่มแท็บ "โจทย์ประยุกต์วิศวกรรม/ICT" แยกจากแบบฝึกหัดปรนัยเดิม — ออกแบบเป็นโจทย์
  ปลายเปิด (Problem Encounter ตามแนว Polya ขั้น 1-2) มีคำถามช่วยคิดก่อน แล้วค่อยกดดู "แนวทางและคำตอบ"
- **MatrixLab.tsx**: เพิ่ม 3 พรีเซ็ตในเมนูโจทย์ตัวอย่าง (วงจรไฟฟ้า, Hill Cipher, ผสมสารเคมี)

### 1.3 ปรับ AI Tutor ให้เป็น Socratic Mentor ตามแนวโพลยาอย่างเคร่งครัด
`server.ts` — เขียน system prompt ของ Gemini ใหม่ทั้งหมด: **ห้ามให้คำตอบสุดท้ายแม้นักเรียนจะขอร้อง**,
ใช้คำถามนำ 1-2 ข้อต่อครั้งตามขั้นของโพลยา, ใช้ Independent Premise Verification เมื่อนักเรียนถามว่า "ถูกไหม",
ถอยระดับคำใบ้ 3 ระดับเมื่อนักเรียนตอบว่า "ทำไม่ได้" — ตรงกับสิ่งที่ระบุไว้ในรายงานวิจัยของโครงการ (หน้า Socratic
Tutor) และตรงกับแนวทางที่ใช้ในบทสนทนานี้เอง พร้อม rebrand UI จาก "Gemini Math Tutor" เป็น **"ครูพี่หนุ่ม AI"**

### 1.4 แก้บั๊กและความสอดคล้องอื่นๆ
- `defaultStudentProgress` เดิมเป็นข้อมูลปลอม (นักเรียนใหม่เห็น XP 1,250 และ badge ที่ไม่ใช่ของตัวเอง) —
  แก้ให้เริ่มจาก 0 อย่างสุจริต
- แก้ข้อความ "10 บทเรียน" ที่เขียนตายตัวในหลายจุด (Dashboard, LearningPath) ให้อ้างอิงจำนวนจริงแบบไดนามิก
- เพิ่มป้ายแจ้งเตือนใน Teacher Dashboard ว่าตารางนักเรียนยังเป็น **ข้อมูลตัวอย่าง (Demo Data)** เนื่องจากยังไม่มี
  ระบบเก็บข้อมูลนักเรียนจริงข้ามเครื่อง (ดูหัวข้อ 2) — ป้องกันความเข้าใจผิดว่าเป็นข้อมูลจริงหากนำไปใช้สอน/นำเสนอ

---

## 2. ข้อค้นพบสำคัญที่ยังไม่ได้แก้ (เก็บไว้พิจารณาเฟส 2)

1. **TeacherDashboard และ Analysis.tsx ใช้ข้อมูลนักเรียน 4 คนแบบ hardcode ทั้งหมด** ไม่ได้อ่านค่าจากใครจริง
   เพราะ `StudentProgress` เก็บใน `localStorage` ของเบราว์เซอร์แต่ละเครื่องเท่านั้น (ตามดีไซน์ "ไม่ต้อง login"
   ในเฟสนี้) ครูจึงยังไม่เห็นพัฒนาการนักเรียนแต่ละคนจริงในภาพรวมชั้นเรียนได้
   - **แนวทางเฟส 2 ที่ยังคง "ไม่ต้อง login"**: ใช้ **รหัสห้องเรียน (Class Code)** แทนบัญชีผู้ใช้ — นักเรียน
     พิมพ์ชื่อ + รหัสห้อง (ไม่มีรหัสผ่าน) ครั้งแรกที่เข้าเรียน ระบบส่งความก้าวหน้าขึ้น backend เบาๆ (เช่น ไฟล์
     JSON หรือ SQLite บน Express ที่มีอยู่แล้ว) แล้วให้ TeacherDashboard ดึงรายชื่อ+คะแนนจริงตามรหัสห้องมาแสดง
     วิธีนี้ยังคงหลักการ "เข้าเรียนได้ทันทีไม่ต้องสมัครสมาชิก" ไว้ครบ เพียงเพิ่ม "ป้ายชื่อ" ให้ข้อมูลแต่ละคนแยกกันได้
2. **`Analysis.tsx` เป็นไฟล์ที่ไม่ได้ใช้งานจริง** (ไม่มีการ route มาเรียก มีแต่ `TeacherDashboard.tsx` ที่ route
   `/analysis`) มีโค้ด mock data ซ้ำซ้อนกับ TeacherDashboard — ควรพิจารณาลบทิ้งหรือรวมเป็นไฟล์เดียวในอนาคต
   เพื่อลดความสับสนเวลาแก้ไขโค้ด (ยังไม่ได้ลบให้ในเฟสนี้ เผื่อพี่มีแผนใช้ไฟล์นี้ต่อ)
3. **Cognitive Offloading ผ่าน Python/NumPy และ WolframAlpha** ที่ระบุไว้ในรายงานวิจัย (Polya ขั้น 3-4)
   ยังไม่มีจุดเชื่อมในแอปจริง (เช่น ปุ่ม "เปิดใน Python/NumPy" หรือ "ตรวจกับ WolframAlpha") — เป็นส่วนที่ทำได้
   โดยไม่ต้องมี login เช่นกัน หากต้องการเพิ่มในเฟสถัดไป
4. ข้อเสนอแนะเดิมใน `PROJECT_SUMMARY.md` (ส่วน "สิ่งที่จะดำเนินการต่อไป") เช่น Export PDF/Excel,
   การแสดงผลเชิงเรขาคณิต 2D/3D, PWA ออฟไลน์, Gamification ขั้นสูง — ยังคงเป็นข้อเสนอแนะที่ใช้ได้ ไม่มีผลกระทบ
   จากงานเฟสนี้ และไม่ต้องมี login เช่นกันหากต้องการทำต่อ

---

## 2.5 [อัปเดต] แก้บั๊กการแสดงผลสมการคณิตศาสตร์ผิดพลาด (KaTeX Rendering)

พบและแก้ไขแล้ว: จุดที่มีทั้ง `\left[ ... \right]` ล้อมรอบ `\begin{array}...\end{array}` (เช่น เมทริกซ์แต่งเติม
`$$\left[\begin{array}{cc|c} 2 & 1 & 5 \\ 1 & -1 & 1 \end{array}\right]$$` ในบทที่ 7, 8, 10) จะแสดงผลเป็น
ข้อความดิบไม่ถูกแปลงเป็นสมการ พร้อมมีบางส่วนขึ้นสีแดง (KaTeX parse error)

**สาเหตุ:** ฟังก์ชัน `preprocessMathText()` ใน `src/components/math/MathComponents.tsx` มีขั้นตอน
"auto-wrap" ที่คอยห่อ `\begin{...}...\end{...}` ที่ยังไม่ได้ห่อด้วย `$...$` ให้อัตโนมัติ แต่เดิมใช้
lookbehind/lookahead ตรวจสอบแค่ตัวอักษรติดกัน 1 ตัว ทำให้เมื่อมี `\left[` คั่นอยู่ระหว่าง `$$` กับ `\begin{...}`
ระบบตรวจไม่พบว่าอยู่ในเครื่องหมาย `$$...$$` เดิมอยู่แล้ว จึงห่อซ้ำซ้อน ผลลัพธ์คือสตริงถูกตัดออกเป็น 3 ท่อน
(`\left[` ค้างเดี่ยว, ข้อความดิบ `\begin{array}...`, และ `\right]` ค้างเดี่ยว) — ท่อนที่ค้างเดี่ยวทำให้ KaTeX
parse ไม่ผ่านและแสดง error สีแดงตามที่พี่เห็นในภาพหน้าจอ

**วิธีแก้:** เขียน `preprocessMathText()` ใหม่ให้แบ่งข้อความเป็นส่วนที่อยู่ใน `$...$`/`$$...$$` อยู่แล้ว
กับส่วนข้อความธรรมดาก่อนเสมอ แล้วให้ auto-wrap ทำงานเฉพาะกับส่วนข้อความธรรมดาเท่านั้น (การันตีว่าจะไม่มีการ
ห่อซ้ำซ้อนอีก ไม่ว่าจะมีคำสั่ง LaTeX อะไรคั่นอยู่ระหว่างเครื่องหมายเดิมก็ตาม)

**การตรวจสอบ:** เพิ่มไฟล์ทดสอบถาวร `src/tests/mathRendering.test.ts` ที่กวาดตรวจสอบทุกข้อความคณิตศาสตร์
ในแอปทั้งหมด (บทเรียนทั้ง 12 บท, โจทย์ประยุกต์ 7 ข้อ, แบบฝึกหัดปรนัย) ผ่าน pipeline เดียวกับที่แอปใช้จริง
(preprocessMathText → แยกส่วน → KaTeX) รวม **332 ข้อความ ผ่านทั้งหมด ไม่มีจุดใดพังหรือมี error ซ่อนอยู่**

## 2.6 [อัปเดต] เอาข้อมูลปลอมในส่วนของครูออกทั้งหมด

ก่อนหน้านี้ TeacherDashboard มีป้าย "ข้อมูลตัวอย่าง" กำกับไว้ แต่ตามที่พี่แจ้งว่าอยากตัดข้อมูลปลอมออกไปเลย
(ไม่ใช่แค่ติดป้าย) จึงลบออกทั้งหมดดังนี้:

- **`src/pages/TeacherDashboard.tsx`**: ลบ array `studentReports` (นักเรียนปลอม 4 คน: สมชาย, กัญญา, ธนากร,
  ณิชา), ลบสถิติ "นักเรียนทั้งหมด 4 คน" และ "ความเชี่ยวชาญเฉลี่ย" ที่คำนวณจากข้อมูลปลอม, ลบตารางคะแนนนักเรียน
  ปลอม, และลบปุ่ม Export CSV ที่จะส่งออกข้อมูลปลอมนั้น เหลือไว้เฉพาะค่าที่เป็นจริง (การตั้งค่าเกณฑ์ความเชี่ยวชาญ/
  ระดับความยาก ซึ่งครูตั้งค่าเองจริง) แทนที่ตารางด้วยข้อความอธิบายตรงไปตรงมาว่าเฟสนี้ยังไม่มีจุดรวมข้อมูล
  นักเรียนรายบุคคล (ตามดีไซน์ไม่ต้อง login) พร้อมชี้ไปที่แผนเฟส 2
- แก้หัวข้อ **"ข้อผิดพลาดเชิงมโนทัศน์ที่พบบ่อย"** ที่เดิมเขียนว่า "วิเคราะห์จากการตอบคำถามของนักเรียนในระบบ"
  (ซึ่งไม่จริง เป็นแค่รายการตายตัว) — เปลี่ยนคำอธิบายให้ตรงความจริงว่าเป็น **ข้อมูลอ้างอิงทั่วไปจากประสบการณ์
  การสอน ไม่ใช่สถิติจากนักเรียนจริง** (เนื้อหายังมีประโยชน์ จึงเก็บไว้ แต่แก้คำกล่าวอ้างที่ผิดออก)
- **ลบไฟล์ `src/pages/Analysis.tsx`** ทิ้งทั้งไฟล์ — เป็นโค้ดที่ไม่ได้ใช้งานจริง (ไม่มี route เรียก) และมี
  ข้อมูลนักเรียนปลอมชุดเดียวกันซ้ำซ้อนอยู่ ยืนยันแล้วว่าไม่มีไฟล์ใดในโปรเจกต์ import ไฟล์นี้ก่อนลบ
- **`src/components/BadgesAndCertificate.tsx`**: แก้ชื่อ fallback ตอนออกใบรับรอง จาก "สมชาย ใจดี" (ชื่อปลอม)
  เป็น "นักเรียนใหม่" (placeholder กลางๆ ตรงกับที่ใช้ใน `defaultStudentProgress`) นักเรียนยังแก้ไขชื่อเองได้
  ก่อนพิมพ์ใบรับรองตามเดิม

ทดสอบแล้วว่า `npm run lint`, `npm run build`, และเทสต์ทั้ง 4 ไฟล์ยังผ่านหมดหลังลบ

## 3. การทดสอบที่ทำแล้ว

รันผ่านทั้งหมดก่อนส่งมอบ:

```powershell
npm install
npm run lint     # tsc --noEmit — ผ่าน ไม่มี type error
npm run build    # vite build + esbuild server — ผ่าน ไม่มี error
npx tsx src/tests/matrixEngine.test.ts          # รวมเคส 4x4 ใหม่ — ผ่านทั้งหมด
npx tsx src/tests/uatAndGenerator.test.ts       # เทสต์เดิม (สุ่ม 100 ระบบ) — ผ่านทั้งหมด
npx tsx src/tests/engineeringProblems.test.ts   # เทสต์ใหม่: โจทย์ประยุกต์ 7 ข้อ — ผ่านทั้งหมด
npx tsx src/tests/mathRendering.test.ts         # เทสต์ใหม่: กวาดตรวจสอบการแสดงผลสมการทั้งแอป (332 ข้อความ) — ผ่านทั้งหมด
```

## 4. วิธีรันต่อบนเครื่อง (PowerShell) และ deploy ผ่าน Google AI Studio

```powershell
# แตกไฟล์ zip ทับโฟลเดอร์โปรเจกต์เดิม (หรือใช้เป็นโฟลเดอร์ใหม่)
cd Interactive-Instructional-Material-on-Matrices
npm install
npm run dev        # ทดสอบระหว่างพัฒนา (ต้องตั้งค่า GEMINI_API_KEY ในไฟล์ .env ก่อน AI Tutor จึงจะตอบได้)
npm run build      # เมื่อพร้อม deploy จริง
npm start          # รันจาก dist/ (production mode)
```

ไฟล์ `.env` (สร้างเองที่ root ของโปรเจกต์ ไม่ต้องอัปโหลดขึ้น GitHub):
```
GEMINI_API_KEY=your_api_key_here
```

เมื่อนำเข้า Google AI Studio ให้ตรวจสอบว่าตั้งค่า environment variable `GEMINI_API_KEY` ในหน้าตั้งค่าของ
โปรเจกต์ด้วย มิฉะนั้น AI Tutor จะตอบข้อความแจ้งเตือนแทน (โค้ดจัดการ error นี้ไว้ให้แล้วใน `server.ts`)
